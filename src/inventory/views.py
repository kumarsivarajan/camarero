#def index():
    #menu admin (por enquanto, link para /admin/inventory)
    #menu design (service_catalog)
    #menu transition (list,service_tree)

#def service_catalog():

#def list(X):
    #lista itens da classe X, com botao de add no inicio e edit no lado de cada item
    #fitros especificos para cada classe X

#def service_tree():
    #exibe o nome de todas depedencias (CIs, Services) que sustentam este servico
    #exibe a dependencia de CPDs.
